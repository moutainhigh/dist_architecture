<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.xpay.facade.notify.entity.NotifyResponse">
    <sql id="table"> tbl_notify_response </sql>

    <!-- 用于返回的bean对象 -->
    <resultMap id="BaseResultMap" type="com.xpay.facade.notify.entity.NotifyResponse">
        <result column="ID" property="id" jdbcType="BIGINT"/>
        <result column="VERSION" property="version" jdbcType="SMALLINT"/>
        <result column="CREATE_TIME" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="NOTIFY_ID" property="notifyId" jdbcType="BIGINT"/>
        <result column="RSP_SIGN" property="rspSign" jdbcType="VARCHAR"/>
        <result column="RSP_TIME" property="rspTime" jdbcType="TIMESTAMP"/>
        <result column="RSP_STATUS" property="rspStatus" jdbcType="SMALLINT"/>
        <result column="RSP_MSG" property="rspMsg" jdbcType="LONGVARCHAR"/>
        <result column="RSP_HISTORIES" property="rspHistories" jdbcType="OTHER"/>
        <result column="EXTRA_INFO" property="extraInfo" jdbcType="OTHER"/>
    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="Base_Column_List">
        ID,
		VERSION,
		CREATE_TIME,
		NOTIFY_ID,
		RSP_SIGN,
        RSP_TIME,
		RSP_STATUS,
		RSP_MSG,
        RSP_HISTORIES,
		EXTRA_INFO
    </sql>

    <!-- 插入记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.xpay.facade.notify.entity.NotifyResponse">
        INSERT INTO
        <include refid="table"/>
        (
        VERSION,
        CREATE_TIME,
        NOTIFY_ID,
        RSP_SIGN,
        RSP_TIME,
        RSP_STATUS,
        RSP_MSG,
        RSP_HISTORIES,
        EXTRA_INFO
        ) VALUES (
        0,
        #{createTime,jdbcType=TIMESTAMP},
        #{notifyId,jdbcType=BIGINT},
        #{rspSign,jdbcType=VARCHAR},
        #{rspTime,jdbcType=TIMESTAMP},
        #{rspStatus,jdbcType=SMALLINT},
        #{rspMsg,jdbcType=LONGVARCHAR},
        #{rspHistories,jdbcType=OTHER},
        #{extraInfo,jdbcType=OTHER}
        )
    </insert>

    <!-- 批量插入记录 -->
    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id" parameterType="list">
        INSERT INTO
        <include refid="table"/>
        (
        VERSION,
        CREATE_TIME,
        NOTIFY_ID,
        RSP_SIGN,
        RSP_TIME,
        RSP_STATUS,
        RSP_MSG,
        RSP_HISTORIES,
        EXTRA_INFO
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            0,
            #{item.createTime,jdbcType=TIMESTAMP},
            #{item.notifyId,jdbcType=BIGINT},
            #{item.rspSign,jdbcType=VARCHAR},
            #{item.rspTime,jdbcType=TIMESTAMP},
            #{item.rspStatus,jdbcType=SMALLINT},
            #{item.rspMsg,jdbcType=LONGVARCHAR},
            #{item.rspHistories,jdbcType=OTHER},
            #{item.extraInfo,jdbcType=OTHER}
            )
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="com.xpay.facade.notify.entity.NotifyResponse">
        UPDATE
        <include refid="table"/>
        <set>
            VERSION = #{version,jdbcType=SMALLINT} + 1,
            NOTIFY_ID = #{notifyId,jdbcType=BIGINT},
            RSP_SIGN = #{rspSign,jdbcType=VARCHAR},
            RSP_TIME = #{rspTime,jdbcType=TIMESTAMP},
            RSP_STATUS = #{rspStatus,jdbcType=SMALLINT},
            RSP_MSG = #{rspMsg,jdbcType=LONGVARCHAR},
            RSP_HISTORIES = #{rspHistories,jdbcType=OTHER},
            EXTRA_INFO = #{extraInfo,jdbcType=OTHER}
        </set>
        WHERE ID = #{id,jdbcType=BIGINT} and VERSION = #{version,jdbcType=SMALLINT}
    </update>

    <!-- 只更新不为null的属性 -->
    <update id="updateIfNotNull" parameterType="com.xpay.facade.notify.entity.NotifyResponse">
        UPDATE
        <include refid="table"/>
        <set>
            VERSION = #{version,jdbcType=SMALLINT} + 1,
            <if test="notifyId != null">
                NOTIFY_ID = #{notifyId,jdbcType=BIGINT},
            </if>
            <if test="rspSign != null">
                RSP_SIGN = #{rspSign,jdbcType=VARCHAR},
            </if>
            <if test="rspTime != null ">
                RSP_TIME = #{rspTime,jdbcType=TIMESTAMP},
            </if>
            <if test="rspStatus != null">
                RSP_STATUS = #{rspStatus,jdbcType=SMALLINT},
            </if>
            <if test="rspMsg != null">
                RSP_MSG = #{rspMsg,jdbcType=LONGVARCHAR},
            </if>
            <if test="rspHistories!=null">
                RSP_HISTORIES = #{rspHistories,jdbcType=OTHER},
            </if>
            <if test="extraInfo != null">
                EXTRA_INFO = #{extraInfo,jdbcType=OTHER}
            </if>
        </set>
        WHERE ID = #{id,jdbcType=BIGINT} and VERSION = #{version,jdbcType=SMALLINT}
    </update>

    <!-- 多条件组合查询 -->
    <select id="listBy" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        <include refid="table"/>
        <where>
            <include refid="condition_sql"/>
        </where>
        <choose>
            <when test="sortColumns != null and sortColumns !='' ">
                <![CDATA[ ORDER BY ${sortColumns} ]]>
            </when>
            <otherwise>
                <![CDATA[ ORDER BY ID DESC ]]>
            </otherwise>
        </choose>
    </select>

    <!-- 根据多条件组合查询，计算总记录数 -->
    <select id="countBy" parameterType="java.util.Map" resultType="long">
        SELECT count(ID) FROM
        <include refid="table"/>
        <where>
            <include refid="condition_sql"/>
        </where>
    </select>

    <!-- 按查询条件删除 -->
    <delete id="deleteBy">
        DELETE FROM
        <include refid="table"/>
        <where>
            <include refid="condition_sql"/>
        </where>
    </delete>

    <!-- 根据多个id查询 -->
    <select id="listByIdList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        <include refid="table"/>
        WHERE ID IN
        <foreach collection="list" item="item" open="(" separator="," close=")">#{item,jdbcType=BIGINT}</foreach>
    </select>

    <!-- 根据id查询 -->
    <select id="getById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        <include refid="table"/>
        WHERE ID = #{id,jdbcType=BIGINT}
    </select>

    <!-- 按id主键删除 -->
    <delete id="deleteById">
        DELETE FROM
        <include refid="table"/>
        WHERE ID = #{id,jdbcType=BIGINT}
    </delete>

    <!-- 按多个id主键删除 -->
    <delete id="deleteByIdList" parameterType="list">
        DELETE FROM
        <include refid="table"/>
        WHERE ID IN
        <foreach collection="list" item="item" open="(" separator="," close=")">#{item,jdbcType=BIGINT}</foreach>
    </delete>

    <!-- ↑↑↑↑↑↑ 如果有新增字段、删除字段、修改字段 以上内容可用模板生成新内容后直接替换 ↑↑↑↑↑↑ -->
    <!-- ↓↓↓↓↓↓ 以下内容是根据需求手动添加或修改的，请勿使用模板内容直接覆盖 ↓↓↓↓↓↓ -->

    <sql id="condition_sql">
        <if test="id != null ">
            AND ID = #{id,jdbcType=BIGINT}
        </if>
        <if test="idList != null and idList.size() > 0">
            AND ID IN
            <foreach collection="idList" item="item" index="index" open="(" separator="," close=")">#{item,jdbcType=BIGINT}</foreach>
        </if>
        <if test="version != null ">
            AND VERSION = #{version,jdbcType=SMALLINT}
        </if>
        <if test="createTime != null ">
            AND CREATE_TIME = #{createTime,jdbcType=TIMESTAMP}
        </if>
        <if test="notifyId != null ">
            AND NOTIFY_ID = #{notifyId,jdbcType=BIGINT}
        </if>
        <if test="rspSign != null and rspSign !='' ">
            AND RSP_SIGN = #{rspSign,jdbcType=VARCHAR}
        </if>
        <if test="rspStatus != null ">
            AND RSP_STATUS = #{rspStatus,jdbcType=SMALLINT}
        </if>
        <if test="rspMsg != null and rspMsg !='' ">
            AND RSP_MSG = #{rspMsg,jdbcType=LONGVARCHAR}
        </if>
        <if test="extraInfo != null ">
            AND EXTRA_INFO = #{extraInfo,jdbcType=OTHER}
        </if>
    </sql>

    <select id="listByNotifyIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        <include refid="table"/>
        WHERE NOTIFY_ID IN
        <foreach collection="notifyIds" item="item" open="(" separator="," close=")">#{item,jdbcType=BIGINT}</foreach>
    </select>


</mapper>

